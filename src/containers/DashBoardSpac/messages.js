/*
 * DashBoardSpac Messages
 *
 * This contains all the text for the DashBoardSpac container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashBoardSpac";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashBoardSpac container!"
  }
});
