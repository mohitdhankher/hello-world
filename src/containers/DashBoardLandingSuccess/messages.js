/*
 * DashBoardLandingSuccess Messages
 *
 * This contains all the text for the DashBoardLandingSuccess container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashBoardLandingSuccess";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashBoardLandingSuccess container!"
  }
});
