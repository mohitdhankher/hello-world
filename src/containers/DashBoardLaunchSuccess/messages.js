/*
 * DashBoardLaunchSuccess Messages
 *
 * This contains all the text for the DashBoardLaunchSuccess container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashBoardLaunchSuccess";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashBoardLaunchSuccess container!"
  }
});
