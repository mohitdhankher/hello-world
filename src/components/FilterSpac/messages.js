/*
 * FilterSpac Messages
 *
 * This contains all the text for the FilterSpac component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.FilterSpac";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FilterSpac component!"
  }
});
