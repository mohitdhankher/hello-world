/*
 * SpacCard Messages
 *
 * This contains all the text for the SpacCard component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.SpacCard";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SpacCard component!"
  }
});
