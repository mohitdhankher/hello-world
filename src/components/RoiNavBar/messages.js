/*
 * RoiNavBar Messages
 *
 * This contains all the text for the RoiNavBar component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RoiNavBar";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RoiNavBar component!"
  }
});
