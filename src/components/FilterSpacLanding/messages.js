/*
 * FilterSpacLanding Messages
 *
 * This contains all the text for the FilterSpacLanding component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.FilterSpacLanding";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FilterSpacLanding component!"
  }
});
